# 创建桌面时用：不加载 tpm/插件，避免 resurrect/continuum 拖死机器
set -g default-terminal "tmux-256color"
set -ag terminal-overrides ",xterm-256color:RGB"
set -g history-limit 2000
set -g status off
set -g mouse on
set -g base-index 0
set -g pane-base-index 1
